{
  "progName": "SandboxIO",
  "htmlText": "<h3>Sandbox File I/O Demo</h3>\n\n<!-- btnLoad calls API to show loader -->\n<button id=\"btnLoad\" onclick=\"API_ShowLoad()\">Load Text</button>\n<button id=\"btnSave\" onclick=\"save_text()\">Save Text</button>\n<br/><br/>\n\n<textarea id=\"txtContents\" rows='8' cols='45'></textarea>\n\n<h4>Press the following button after you have loaded a text file</h4>\n<button id=\"btnLastLoad\" onclick=\"log_last_load()\">Log Loaded File</button>",
  "scriptText": "\n// Implement this callback to get notified when\n// file loading is complete.\nfunction EVT_UserLoadCallback(result) \n{\n\t$(\"#txtContents\").val(result);\n\tsb_user_hide_load();  // api to hide loader\n}\n\n// this function could have been called anything\n// but within it it calls sb_user_save which is API\nfunction save_text()\n{\n    var fileName = \"myfile.txt\";\n    var fileText = $(\"#txtContents\").val();\n    \n    // Sandbox API call to save a file\n    API_SaveTextFile(fileName, fileText);\n}\n\n// In case you dont need asyncronous notification\n// The output was stored in global api var\nfunction log_last_load()\n{\n\tAPI_LogMessage(VAR_UserFileValue);\n\tAPI_SetActiveTab(2);\n}"
}